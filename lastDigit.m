function y = lastDigit(a,b)
% Author: Mehreen Asad
% Checks to see if rightmost digits of two integers are same

if(mod(a,10) == mod(b,10))
    %|| mod(a,10) == mod(c,10) || mod(b,10) == mod(c,10))
    

    y = true;

else
    y = false;
    
end
end
